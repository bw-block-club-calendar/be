// const cleaner = require('knex-cleaner');

exports.seed = function(knex, Promise) {
  // return cleaner.clean(knex); // cleans all tables and resets primary keys
  // must find way to ignore knex_migrations and knex_migrations_lock
}